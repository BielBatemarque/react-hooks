import { SectionTitle } from '../../components/layout/SectionTitle';

export const UseCallback = () => {
    return(
        <div className="useCallback">
            <SectionTitle title={'Hook useCallback'} />
                <h3>Retorna uma função memorizada</h3>
        </div>
    );
};